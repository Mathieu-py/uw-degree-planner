import { SettingsView } from "@/components/settings/SettingsView";

export const metadata = {
  title: "Settings · UW Degree Planner",
};

export default function SettingsPage() {
  return <SettingsView />;
}
